
<div class="container">
  <div class="row voffset2">
    <div class="col-xs-12 col-sm-8 col-sm-offset-2">
      <div class="panel panel-default">
        <div class="panel-body">
          <h3 id="title">Would you like to recieve a lovely text message? </h3>  

          <%= simple_form_for @message, url: root_path do |f| %>
            <h4 class="voffset2">Enter your phone number here</h4>
            <div class="row">
              <div class="form-group col-xs-12 col-sm-6">
                <%= f.input :phone_number, placeholder: 'Phone Number', label: false %>
              </div>
            </div>  

            <%= f.submit 'Submit', class: 'btn btn-lrg btn-success voffset1' %>
          <% end %>
          <br />
          <% if flash[:notice] %>
            <span class="valid-number"><%= flash[:notice] %></span>
          <% end %>
          <% if flash[:error] %>
            <span class="non-valid-number"><%= flash[:error] %></span>
          <% end %>
        </div>
      </div>
    </div> 
  </div>
</div>

 
